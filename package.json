{
  "name": "mermaid-to-slides",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Turn Mermaid flowcharts into editable PowerPoint shapes, entirely in your browser.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "@dagrejs/dagre": "3.1.1",
    "jszip": "3.10.1",
    "pptxgenjs": "4.0.1"
  },
  "devDependencies": {
    "@xmldom/xmldom": "^0.9.12",
    "esbuild": "^0.28.1",
    "jsdom": "^29.1.1",
    "vite": "^7.3.6"
  },
  "engines": {
    "node": ">=22.13"
  }
}
